<!--Pantalla de LogIn-->
<head>
    <title>{{{title}}}</title>
</head>


<body id="insColor">
    <section style="padding-top: 20px;">
        <div class="col-sm-6 offset-sm-3">
        <div class="loginForm">

            {{#if mensaje}}
            <p class="mensaje mensaje-{{tipo}}">{{mensaje}}</p>
            {{/if}}

            <h2>Bienvenido Farmacéutico!</h2>
            <p>Gestionamos tus reactivos y las drogas preparadas.
            Ademas tenemos miles de Recetas Magistrales para consulta.</p>    
            <p>Ingresa tu Usuario y Clave para entrar a la aplicación</p>  

            <form action="/login" method="post">
            <div class="form-group">
                <label for="user">Usuario</label>
                <input type="text" class="form-control" name="user" placeholder="User">
            </div>
            <div class="form-group">
                <label for="password">Clave</label>
                <input type="password" class="form-control" name="password" placeholder="Password">
            </div>
                <a href="home"><button type="submit" class="btn btn-info" id="insColor">Entrar</button></a>
            </form>
        </div>
        </div>
    </section>
</body>
</html>

